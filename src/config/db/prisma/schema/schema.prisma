generator client {
  provider        = "prisma-client-js"
  output   = "../generated/simestadb"
}

datasource db {
  provider          = "postgres"
  url               = env("DATABASE_URL")
  // shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
}
